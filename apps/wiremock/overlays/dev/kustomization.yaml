apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Sin bloque `images:` — a diferencia de nuestros propios servicios,
# WireMock no tiene pipeline propio: es una imagen pública de terceros,
# fija directamente en base/deployment.yaml (misma versión en todos los
# ambientes, no hay tag por commit que sobreescribir aquí).
resources:
  - ../../base
